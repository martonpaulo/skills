interface:
  display_name: "Architecture Audit"
  short_description: "Assess architecture from concrete code evidence"
  default_prompt: "Use $architecture-audit to assess this codebase and rank evidence-backed architecture improvements."
policy:
  allow_implicit_invocation: false
