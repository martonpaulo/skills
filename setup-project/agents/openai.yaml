interface:
  display_name: "Setup Project"
  short_description: "Set up new or existing repositories"
  default_prompt: "Use $setup-project to set up or align this repository with my project rules and one-time decisions."
policy:
  allow_implicit_invocation: false
