interface:
  display_name: "Bug Audit"
  short_description: "Find verified behavioral and security bugs"
  default_prompt: "Use $bug-audit to audit this code for verified functional, logic, runtime, and security bugs without fixing them."
policy:
  allow_implicit_invocation: false
