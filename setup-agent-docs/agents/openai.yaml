interface:
  display_name: "Setup Agent Docs"
  short_description: "Configure lightweight repository artifact paths"
  default_prompt: "Use $setup-agent-docs to configure only the optional artifact paths this repository needs."
policy:
  allow_implicit_invocation: false
