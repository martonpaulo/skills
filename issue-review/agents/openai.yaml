interface:
  display_name: "Review Pull Request"
  short_description: "Validate one PR against all closing issues"
  default_prompt: "Use $issue-review to validate this pull request against every issue it closes."
policy:
  allow_implicit_invocation: false
