interface:
  display_name: "Review Pull Request"
  short_description: "Validate one PR against all closing issues"
  default_prompt: "Use $issue-review with an issue number or URL, a PR URL, or pr followed by a PR number."
policy:
  allow_implicit_invocation: false
