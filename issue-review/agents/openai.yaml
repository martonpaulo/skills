interface:
  display_name: "Review Pull Request"
  short_description: "Validate one pull request before merge"
  default_prompt: "Use $issue-review to validate this pull request."
policy:
  allow_implicit_invocation: false
