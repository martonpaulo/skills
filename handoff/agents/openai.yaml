interface:
  display_name: "Handoff"
  short_description: "Write compact context for a later session"
  default_prompt: "Use $handoff to write a compact continuation note for the next session."
policy:
  allow_implicit_invocation: false
