interface:
  display_name: "Interface Audit"
  short_description: "Audit product UI, UX, accessibility, and copy"
  default_prompt: "Use $interface-audit medium to audit this product UI, UX, accessibility, and copy without changing it."
policy:
  allow_implicit_invocation: false
