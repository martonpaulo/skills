interface:
  display_name: "Plan Issue"
  short_description: "Produce the plan and tasks for one issue"
  default_prompt: "Use $issue-plan with an issue number or URL to write its plan and tasks."
policy:
  allow_implicit_invocation: false
